import type { TldrawOptions } from "tldraw";

export const TLDRAW_OPTIONS: Partial<TldrawOptions> | undefined = {
  maxPages: 1,
  // Keep it simple for Windows 98 look
  persistenceKey: "meme-board-windows98"
}