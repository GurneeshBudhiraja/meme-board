import {
  type TLComponents,
  type TldrawOptions,
} from "tldraw";

export const TLDRAW_OPTIONS: Partial<TldrawOptions> | undefined = {
  maxPages: 1,
};



export const TLDRAW_COMPONENTS: TLComponents = {
  MainMenu: null,
};
